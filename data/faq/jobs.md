# Job ingestion

Use the **Jobs** tab to paste a casting brief. OLC parses it into a structured requirement, checks for similar open jobs (dedupe), and lets you confirm to create a new REQ. Then open Match and run shortlisting against the new requirement.
